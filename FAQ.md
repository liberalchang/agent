# FAQ（高频问题口径）

## 1. 什么时候需要用 RAGFlow？

当问题涉及项目内约定、文档细节、版本差异或需要依据的结论时，应优先检索增强。详见 `RAGFLOW_POLICY.md`。

## 2. RAGFlow 不可用怎么办？

进入降级模式：

- 优先使用本仓库 Markdown 文件
- 明确说明依据不足或检索不可用
- 给出需要补充的信息或建议的检索关键词

## 3. 信息冲突时以谁为准？

遵循优先级：当前会话 > 身份层 > 行为层 > 记忆层 > 环境/引导 > RAGFlow > 通用先验。详见 `SOUL.md`。
