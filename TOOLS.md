# TOOLS（环境层：工具与依赖）

## 目标

明确可用工具、外部依赖、使用约束与故障降级策略，避免“能力幻觉”。

## RAGFlow（知识检索增强）

- 用途：在需要依据、项目内约定、版本敏感信息时进行检索增强
- 约束：检索结果需要筛选、归纳，并与本地文档优先级对齐
- 输出要求：尽量标明来源与可信度

## 故障与降级

当 RAGFlow 不可用、检索为空或冲突时：

- 优先使用本仓库 Markdown 文档
- 明确说明“检索不可用/检索不足/冲突存在”
- 给出下一步建议（关键词、需要补充的信息、需要确认的版本）
